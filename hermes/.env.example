# Read by hermes/skills/parent-health-onboarding/scripts/backend_client.py.
# Set this in whatever environment runs the Hermes profile handling the
# parent-health-agent WhatsApp number (defaults to http://127.0.0.1:8000 if unset).
PARENT_HEALTH_API_BASE_URL=http://127.0.0.1:8000
